services:
  app:
    build: .
    ports:
      - "${PORT:-5000}:5000"
    volumes:
      - equipment-data:/app/data
    environment:
      - DATABASE_PATH=/app/data/equipment.db
      - UPLOAD_PATH=/app/data/uploads

volumes:
  equipment-data:
